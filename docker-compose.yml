version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: credit_simulator_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: credit_simulator
      MYSQL_USER: credit_user
      MYSQL_PASSWORD: credit_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

volumes:
  mysql_data:
